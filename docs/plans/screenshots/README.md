# Slice 7 reference screenshots

Three PNGs (album-detail, now-playing, library) are placeholders —
1×1 transparent stubs committed so the file paths exist for slice 7
§8 step 14. Replace them with real captures from the Pixel 9 Pro
Fold outer display once the app boots with bundled fonts (see
`apps/mobile/pubspec.yaml`'s `TODO(slice-7-fonts)` block).

§11 verification 10 ("Aurora variant: each top-level screen's
backdrop matches the reference PNG within ±5 LAB") is the gate that
needs the real captures.
